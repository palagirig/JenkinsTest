({
	checkValid : function(component, event, helper){
    helper.checkValidCmp(component);
  },

  doHandleRevenueChange : function(component, event, helper){
      helper.handleRevenueChange(component);
  }
})